# next / trainpipe — Implementierungsfortschritt

Ziel: Die im Architektur-Review identifizierten Lücken gegen die Produktziele
schließen (einfaches Training von FT/Pretraining/RL/GRPO/HP-Tuning, voll über
UI bedienbar, CLI + MCP agent-tauglich).

Branch: claude/simplify-b94tk5
Vorgehen: pro Punkt -> implementieren -> /simplify -> committen -> dieses File updaten.

## Punkteliste
0. [DONE] MCP-Eval-Tools (train->eval->improve-Loop fuer Agents)  — commit 0d3716e
1. [DONE] RL/GRPO erstklassig  — commit 7238b78
   - RLHFHyperparameters (beta, reward_model, reward_funcs, num_generations,
     max_completion_length, temperature) -> swift rlhf-Flags.
   - ExperimentSpec._check_rlhf-Validator: reward-Signal fuer ppo/grpo am
     Submit erzwungen; GRPO-only-Knobs gekapselt; rlhf fuer non-RLHF verboten.
   - swift_builder bleibt reine Translation-Schicht.
   - Tests: Flag-Emission + alle Validierungsregeln. /simplify: 2 Fixes
     (RLHF_KINDS jetzt genutzt, Nesting geglättet), 2 bewusst skipped.
2. [DONE] Pretraining-Pfad  — commit cb1ef30
   - 'pt' in TrainKind; swift_builder routet auf `swift pt` (LoRA + full).
   - is_pretrain-Erkennung (text-Feld) in dataset_formats, informativ wie
     is_preference. /simplify: clean, 1 Vorschlag bewusst skipped.
3. [DONE] Operative CLI  — commit b075e45
   - trainpipe/client.py (shared httpx-Client + unwrap), von CLI UND MCP genutzt
     (mcp.py entduppliziert).
   - trainpipe/cli.py: serve (default) + submit/experiments/get/cancel/logs/
     datasets/upload/models/register-model/set-alias/inference/studies/gpus/
     eval-suites/create-suite/run-eval/eval-runs/eval-results/compare-evals
     + generischer `api METHOD PATH` (volle Parity). JSON-Ausgabe.
   - README erweitert. /simplify: 3 Fixes (Path.name, except-Klauseln,
     2 Shortcuts), 2 bewusst skipped.
4. [IN PROGRESS] UI: gefuehrter Train-Wizard + Pipeline-Erstellungsformular.

## Log
- 2026-06-27: Review abgeschlossen. Punkt 0 (MCP-Eval-Tools) gepusht (0d3716e).
- 2026-06-27: Punkt 1 (RL/GRPO) fertig + /simplify + commit 7238b78.
  Volle Testsuite gruen (480+ Tests). Starte Punkt 2 (Pretraining).
- 2026-06-27: Punkt 2 (Pretraining) fertig + /simplify + commit cb1ef30.
  489 Tests gruen. Starte Punkt 3 (CLI).
- 2026-06-27: Punkt 3 (CLI) fertig + /simplify + commit b075e45.
  507 Tests gruen. Starte Punkt 4 (UI).
