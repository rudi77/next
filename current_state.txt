# next / trainpipe — Implementierungsfortschritt: Agentic Data Acquisition

Ziel: Neues Modul `acquisition` bauen, das aus einem natürlichsprachigen
Auftrag ein Trainingsset konstruiert (Recherche → Web-Akquise → Synthese →
Curation → Register). Design-Doc: docs/spec/agentic-data-acquisition.md.

Branch: claude/nextjs-capabilities-ole3jb
Vorgehen: pro Stufe -> implementieren -> /simplify -> committen -> dieses File updaten.

## Plan (4 Stufen aus dem Design-Doc §12)

1. [DONE] MVP-Gerüst: Modul + acquisition_runs-Migration + Route + MCP-Tool +
       Driver/Manager mit Mock-Providern; Phasen Intake, Synthesize
       (from-scratch), Curate (Dedup+Validate), Register. Noch ohne echtes Web.
2. [DONE] Async-Klärung: awaiting_input + PATCH …/answers.
       (Kam mit Stufe 1, weil intrinsisch zur State-Machine.)
3. [DONE] Research/Acquire real: SearchProvider (mock/Tavily) + Extractor
       (trafilatura/SimpleExtractor) + Robots/Lizenz/SSRF-Gate; [acquisition]-Extra.
4. [DONE] Härtung: Redaction-Pflichtstufe (curate, rekursiv), Cost-Budget
       (max_llm_calls), strict_license-Knopf, Lineage via Provenance +
       acquisition_runs.dataset_id, UI-Tab.

## ALLE 4 STUFEN ABGESCHLOSSEN ✅

## Was steht (Stufe 1+2)

- Migration v14: acquisition_runs + acquisition_sources (db.py).
- Schemas: AcquisitionStatus/Phase/Spec/Source/Request/Answers/Run +
  ACQUISITION_TERMINAL_STATUSES (api/schemas.py).
- Repository: create/get/list/list_active/update_acquisition_run +
  record/list_acquisition_sources (core/repository.py).
- Modul trainpipe/acquisition/: runner.py (Phasen: intake, synthesize,
  curate, write_records_jsonl — reuse synth.SynthProvider), driver.py
  (In-Process-Phasen-Task, awaiting_input-Pause/Resume), manager.py
  (Lifecycle + answer/cancel + _remove_driver).
- Route api/routes/acquisitions.py: POST / GET-list / GET-one /
  GET-sources / PATCH answers (404/409) / POST cancel.
- MCP: start/get/list/answer/cancel_acquisition (mcp.py).
- Wiring: deps.get_acquisition_manager, main.py lifespan + router include.
- Tests: tests/test_phase22_acquisition.py (18 Tests: pure functions,
  driver end-to-end, awaiting_input-Resume, manager, REST).

## Verifikation (Stufe 1+2)

- Volle Testsuite grün: 522 passed, 1 skipped.
- ruff sauber auf allen neuen/geänderten Dateien (41 pre-existing
  Repo-Fehler in fremden Dateien unberührt).
- /simplify durchlief 4 Cleanup-Agents; 7 Findings angewendet
  (Terminal-Set zentralisiert, _fallback_domain, JSON-Extract vereinfacht,
  _remove_driver-Helper, Provider 1×, datengetriebener Updater, Manager
  gibt Run zurück → Routes ohne Re-Read). Cross-Modul-Reuse + Design-
  Entscheidungen bewusst übersprungen.

## Was steht (Stufe 3)

- trainpipe/acquisition/web.py: SearchProvider (Mock/Tavily, Tavily mit
  synth._post_with_retry), Extractor (Trafilatura/SimpleExtractor),
  make_fetch_gate (Lizenz-Heuristik → SSRF-Check via labelstudio
  _validate_base_url → robots.txt, robots pro Host memoisiert, alles
  injizierbar für Offline-Tests).
- runner.py: research_sources (Query-Budget, Dedup, Gate), acquire_records
  (fetch+LLM-Destillat, used-Flag in place).
- driver.py: _research_and_acquire (research→acquire→Source-Ledger);
  target_count bleibt Gesamtbudget (Synth füllt nur Rest).
- Migration v15: search_provider + max_sources auf acquisition_runs.
- Schema/Repository/Route/MCP: search_provider + max_sources durchgereicht;
  MCP get_acquisition_sources; pyproject [acquisition]-Extra (trafilatura).
- Tests: +9 (web-Primitiven, Gate robots/SSRF/Lizenz, research/acquire,
  Driver-Web-Pfad). Gesamt 531 passed.

## Was steht (Stufe 4 — Härtung)

- Redaction-Pflichtstufe: curate redaktiert jeden Record (rekursiv via
  redaction.redact_record/_walk_redact) VOR Dedup+Register; Hit-Counts
  persistiert (redaction_json). Verschachtelte PII (Chat-Format) erfasst.
- Cost-Budget: _BudgetProvider zählt LLM-Calls, max_llm_calls kappt
  Synthese+Acquire (per-Call gepollt, kein Overshoot).
- strict_license: Gate-Knopf, überspringt Quellen mit unbestätigter Lizenz.
- Lineage: Provenance-String + acquisition_runs.dataset_id (Run→Dataset)
  + acquisition_sources (Run→URLs). dataset_lineage-Tabelle nicht nutzbar
  (kein Parent-Dataset bei Web-Quellen).
- curate gibt CurateStats-Dataclass zurück (statt 3-Tuple).
- Migration v16: strict_license + max_llm_calls + redaction_json.
- UI: Acquisition-Tab (Brief-Form, Runs-Liste mit Status/Phase,
  Cancel/Answer für awaiting_input). MCP get_acquisition_sources.
- Tests gesamt: 536 passed, 1 skipped.

## Bekannte Folgearbeit (globale Cleanups, modulübergreifend)

- Reuse: synth.generate_synthetic Failure-Loop und JSON-Extract aus
  evals/metrics/llm_as_judge konsolidieren.
- Effizienz: pooled httpx.Client + concurrent fetch über research/acquire;
  redact_text Doppelpass (count+sub) in redactor.py zusammenfassen.

## Log

- 2026-06-27: Design-Doc committet (docs/spec/agentic-data-acquisition.md).
- 2026-06-27: Stufe 1+2 implementiert + /simplify + Commit 9c73ab7.
- 2026-06-27: Stufe 3 (Web-Research/Acquire) + /simplify + Commit 9dd26d7.
- 2026-06-27: Stufe 4 (Härtung + UI) + /simplify + Commit folgt. FERTIG.
